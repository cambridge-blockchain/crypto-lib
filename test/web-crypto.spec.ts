import * as _ from 'lodash';

import { CBCWebCrypto } from '../src/cbc-web-crypto';
import * as assert from 'assert';

describe('Asymmetric key generation', () => {
    it('should resolve to true', () => {
        assert.equal(1, 1);
    });
});
